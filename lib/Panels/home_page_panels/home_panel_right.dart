import 'package:flutter/material.dart';
class HomePanelRight extends StatelessWidget {
  const HomePanelRight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Panel Right From Home Page"),
    );
  }
}
