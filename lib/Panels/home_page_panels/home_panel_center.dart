import 'package:flutter/material.dart';
class HomePanelCenter extends StatelessWidget {
  const HomePanelCenter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Panel Center From Home Page"),
    );
  }
}
